CREATE TABLE workouts(date DATE, seq INTEGER, wod_section VARCHAR, updated_at TIMESTAMP, wod_title VARCHAR, workout_name VARCHAR, wod_results_count INTEGER, wod_results_url VARCHAR, workout_description VARCHAR, PRIMARY KEY(date, seq));;

